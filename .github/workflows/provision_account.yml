name: Provision account
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: "*/5 * * * *"

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - id: 0

    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check how many accounts are provisioned
        run: |
          echo "Number of provisioned accounts: ${curl https://polygongas.org/keys/count}"

      - name: Prepare funding
        run: |
          wget https://github.com/golemfactory/erc20_payment_lib/releases/download/pre-rel-v0.4.8-nightly/erc20_processor-linux-x86_64.tar.xz
          tar -xf erc20_processor-linux-x86_64.tar.xz
          sudo cp erc20_processor /usr/bin/erc20_processor
          erc20_processor --version

      - name: Generate ethereum accounts
        run: |
          mkdir funding && cd funding
          erc20_processor generate-key -n 1 > .env
          erc20_processor get-dev-eth
          erc20_processor mint-test-tokens
          erc20_processor run
          erc20_processor transfer --recipient 0x5550000000000000000000000000000000000555 --token glm amount 900
          erc20_processor run
          erc20_processor balance
          
          cat .env | grep ETH_ADDRESS_ | sed "s/#\s//g" | sed "s/:\s/=/g" >> $GITHUB_ENV
          cat .env | grep ETH_PRIVATE_KEY_ | sed "s/#\s//g" | sed "s/:\s/=/g" >> $GITHUB_ENV

      - name: Upload account
        run: |
          curl -X POST -H "Authorization: Bearer ${{ secrets.PRIVATE_KEY_SERVER_KEY }}" -d $ETH_PRIVATE_KEY_0 https://polygongas.org/keys/add
